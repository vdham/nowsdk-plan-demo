// Placeholder server-side script. Body is intentionally trivial —
// the demo cares about metadata (when/active/table), not runtime logic.
(function executeRule(current, previous) {
    // no-op
})(current, previous);
