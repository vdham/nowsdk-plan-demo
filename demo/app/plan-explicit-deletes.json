{
  "_comment": "Deletions are not expressible in Fluent source (removing a field from a Table() literal is not a signal to DROP it on the instance). This sibling file explicitly lists resources the local app intends to delete, per PRD §8 step 2.",
  "explicitDeletes": [
    {
      "id": "field:x_helloworld_tableone.datetime_field",
      "type": "field"
    }
  ]
}
