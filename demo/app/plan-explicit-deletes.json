{
  "_comment": "The prototype does not infer deletes from absence — this file makes delete intent explicit for the demo. Production `plan` would derive delete semantics from the SDK's install-time change representation (now-sdk build --generate-deletes, default true). See PRD §8 step 2.",
  "explicitDeletes": [
    {
      "id": "field:x_helloworld_tableone.datetime_field",
      "type": "field"
    }
  ]
}
